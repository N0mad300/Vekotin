<Window x:Class="Vekotin.WidgetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:local="clr-namespace:Vekotin"
        mc:Ignorable="d"
        Title="WidgetWindow" 
        Height="450" 
        Width="800"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.ContextMenu>
        <ContextMenu>
            <MenuItem x:Name="ClickThroughMenuItem" 
                     Header="Enable Click-Through" 
                     Click="ToggleClickThrough_Click"/>
            <MenuItem Header="Open DevTools" 
                     Click="OpenDevTools_Click"/>
            <Separator/>
            <MenuItem Header="Close Widget" 
                     Click="CloseWidget_Click"/>
        </ContextMenu>
    </Window.ContextMenu>

    <Border CornerRadius="8" 
            Background="Transparent"
            ClipToBounds="True">
        <wv2:WebView2 x:Name="WebView" 
                      DefaultBackgroundColor="Transparent"/>
    </Border>
</Window>
